@extends('layouts/storefront')

@section('title', 'FAQ — Stamped')

<script server>
// Marker pairs — substituted per-locale after render.
const faqs = [
  { q: '{t:faq.q1}', a: '{t:faq.a1}' },
  { q: '{t:faq.q2}', a: '{t:faq.a2}' },
  { q: '{t:faq.q3}', a: '{t:faq.a3}' },
  { q: '{t:faq.q4}', a: '{t:faq.a4}' },
  { q: '{t:faq.q5}', a: '{t:faq.a5}' },
  { q: '{t:faq.q6}', a: '{t:faq.a6}' },
]
</script>

@section('content')
  <div class="mx-auto max-w-3xl px-6 py-16">
    <header class="text-center">
      <p class="text-[11px] font-semibold uppercase tracking-[0.22em] text-stone-500">{t:faq.eyebrow}</p>
      <h1 class="mt-3 font-display text-5xl text-stone-900 md:text-6xl">{t:faq.title}</h1>
    </header>

    <div class="mt-12 divide-y divide-stone-900/10 border-y border-stone-900/10">
      @foreach (faqs as item)
        <details class="group py-5">
          <summary class="flex cursor-pointer list-none items-center justify-between gap-4 text-lg text-stone-900">
            <span class="font-display">{{ item.q }}</span>
            <span class="shrink-0 text-stone-400 transition-transform group-open:rotate-45">+</span>
          </summary>
          <p class="mt-3 leading-relaxed text-stone-600">{{ item.a }}</p>
        </details>
      @endforeach
    </div>

    <div class="mt-12 rounded-sm bg-[#efe7d4] p-8 text-center">
      <p class="font-display text-2xl text-stone-900">{t:faq.cta_title}</p>
      <p class="mt-2 text-sm text-stone-600">{t:faq.cta_copy}</p>
      <a href="/contact" data-stx-link class="mt-5 inline-flex border border-stone-900 px-8 py-3 text-[11px] font-semibold uppercase tracking-[0.22em] text-stone-900 transition-colors hover:bg-stone-900 hover:text-[#f7f1e3]">{t:faq.cta_button}</a>
    </div>
  </div>
@endsection
