@extends('layouts/storefront')

@section('title', 'FAQ — Stamped')

<script server>
const faqs = [
  { q: 'How do your pieces fit?', a: 'Our tees are cut boxy and relaxed — if you prefer a classic fit, size down one. Hoodies are oversized with dropped shoulders. Each product page lists the exact fit and fabric weight.' },
  { q: 'When will my order ship?', a: 'In-stock orders ship within two business days from New York. You\'ll get a tracking link by email as soon as it\'s on its way.' },
  { q: 'Do you ship internationally?', a: 'Yes. International shipping is calculated at checkout. Duties and taxes may be collected by your local carrier on delivery.' },
  { q: 'What\'s your return policy?', a: 'Unworn, unwashed items can be returned within 30 days for a refund or exchange. Start a return from your account or by emailing us.' },
  { q: 'How should I care for my pieces?', a: 'Machine wash cold inside-out, hang or tumble dry low. Skip the iron on the print. Our heavyweight cotton is pre-shrunk, so it holds its shape.' },
  { q: 'Will sold-out pieces come back?', a: 'Sometimes. We make in small batches, so popular pieces restock occasionally — join the list to hear first.' },
]
</script>

@section('content')
  <div class="mx-auto max-w-3xl px-6 py-16">
    <header class="text-center">
      <p class="text-[11px] font-semibold uppercase tracking-[0.22em] text-stone-500">Good to know</p>
      <h1 class="mt-3 font-display text-5xl text-stone-900 md:text-6xl">FAQ</h1>
    </header>

    <div class="mt-12 divide-y divide-stone-900/10 border-y border-stone-900/10">
      @foreach (faqs as item)
        <details class="group py-5">
          <summary class="flex cursor-pointer list-none items-center justify-between gap-4 text-lg text-stone-900">
            <span class="font-display">{{ item.q }}</span>
            <span class="shrink-0 text-stone-400 transition-transform group-open:rotate-45">+</span>
          </summary>
          <p class="mt-3 leading-relaxed text-stone-600">{{ item.a }}</p>
        </details>
      @endforeach
    </div>

    <div class="mt-12 rounded-sm bg-[#efe7d4] p-8 text-center">
      <p class="font-display text-2xl text-stone-900">Still have a question?</p>
      <p class="mt-2 text-sm text-stone-600">We're happy to help with sizing, orders, and anything else.</p>
      <a href="/contact" data-stx-link class="mt-5 inline-flex border border-stone-900 px-8 py-3 text-[11px] font-semibold uppercase tracking-[0.22em] text-stone-900 transition-colors hover:bg-stone-900 hover:text-[#f7f1e3]">Contact us</a>
    </div>
  </div>
@endsection
