@extends('layouts/storefront')

@section('title', 'Returns — Stamped')

@section('content')
  <article class="mx-auto max-w-2xl px-6 py-16">
    <p class="text-[11px] font-semibold uppercase tracking-[0.22em] text-stone-500">{t:policies.label}</p>
    <h1 class="mt-3 font-display text-5xl text-stone-900">{t:policies.returns_title}</h1>
    <p class="mt-4 text-sm text-stone-500">{t:policies.sample_note}</p>

    <div class="mt-10 space-y-8 leading-relaxed text-stone-600">
      <section>
        <h2 class="font-display text-2xl text-stone-900">{t:policies.returns_h1}</h2>
        <p class="mt-2">{t:policies.returns_p1}</p>
      </section>
      <section>
        <h2 class="font-display text-2xl text-stone-900">{t:policies.returns_h2}</h2>
        <p class="mt-2">{t:policies.returns_p2}</p>
      </section>
      <section>
        <h2 class="font-display text-2xl text-stone-900">{t:policies.returns_h3}</h2>
        <p class="mt-2">{t:policies.returns_p3}</p>
      </section>
      <section>
        <h2 class="font-display text-2xl text-stone-900">{t:policies.returns_h4}</h2>
        <p class="mt-2">{t:policies.returns_p4}</p>
      </section>
    </div>
  </article>
@endsection
