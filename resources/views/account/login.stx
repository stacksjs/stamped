@extends('layouts/storefront')

@section('title', 'Sign in — Stamped')

@section('content')
  <div class="mx-auto max-w-md px-6 py-20">
    <header class="text-center">
      <h1 class="font-display text-5xl text-stone-900">Sign in</h1>
      <p class="mt-3 text-stone-600">Welcome back. Sign in to track orders and check out faster.</p>
    </header>

    <form action="/account" method="get" class="mt-10 space-y-5">
      <div>
        <label class="text-[11px] font-semibold uppercase tracking-[0.16em] text-stone-500">Email</label>
        <input type="email" name="email" required autocomplete="email" class="mt-1.5 w-full border border-stone-900/20 bg-transparent px-4 py-3 text-sm focus:border-stone-900 focus:outline-none">
      </div>
      <div>
        <div class="flex items-center justify-between">
          <label class="text-[11px] font-semibold uppercase tracking-[0.16em] text-stone-500">Password</label>
          <a href="/account/reset" data-stx-link class="text-[11px] uppercase tracking-[0.12em] text-stone-500 hover:text-stone-900">Forgot?</a>
        </div>
        <input type="password" name="password" required autocomplete="current-password" class="mt-1.5 w-full border border-stone-900/20 bg-transparent px-4 py-3 text-sm focus:border-stone-900 focus:outline-none">
      </div>
      <button type="submit" class="w-full bg-stone-900 px-8 py-4 text-[11px] font-semibold uppercase tracking-[0.22em] text-[#f7f1e3] transition-colors hover:bg-stone-700">Sign in</button>
    </form>

    <p class="mt-8 text-center text-sm text-stone-600">
      New here? <a href="/account/register" data-stx-link class="font-medium text-stone-900 underline underline-offset-4">Create an account</a>
    </p>
  </div>
@endsection
